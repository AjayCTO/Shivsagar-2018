@using SHIVAM_ECommerce.Models;
@using Microsoft.AspNet.Identity;
@{
    ViewBag.Title = "Manage Account";
    var statusmessage= ViewBag.StatusMessage;
}

<div class="row page-titles">


    <div class="col-md-5 align-self-center">
        <h3 class="text-primary">Settings</h3>
    </div>
    <div class="col-md-7 align-self-center">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/Home">Home</a></li>
            <li class="breadcrumb-item active">Manage Account</li>
        </ol>
    </div>
</div>

@*@if (statusmessage == " "||statusmessage == null)
{
   
}
else{
   <div class="text-success alert alert-success alert-dismissable">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button> @ViewBag.StatusMessage  </div>
}*@
<div class="container-fluid">
    <div class="row">
        <div class="col-md-12">


            <div class="card">
                <div class="card-header">
                    <h3>Update your account password</h3>
                </div>
                <div class="card-body">
                  

                    <div class="row">
                        <div class="col-md-12">
                            @if (ViewBag.HasLocalPassword)
                {
                                @Html.Partial("_ChangePasswordPartial")
                }
                else
                {
                                @Html.Partial("_SetPasswordPartial")
                }

                        </div>
                    </div>
                </div>
            </div>

        </div>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
